<Window x:Class="m326.View.CompetenceEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:m326.ViewModel"
        mc:Ignorable="d"
        Title="CompetenceEditWindow" Height="450" Width="800">

    <Window.DataContext>
        <vm:CompetenceEditView/>
    </Window.DataContext>
    <Grid Margin="10">
        <StackPanel>
            <Button Margin="2" Padding="2" Width="60" HorizontalAlignment="Left">Zurück</Button>
            <TextBlock FontWeight="Bold">Kompetenz bearbeiten</TextBlock>
            <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                <TextBlock>Name:</TextBlock>
                <TextBox Margin="10 0 0 0" Width="300" Text="{Binding Competence.Title}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                <TextBlock>Kompetenzbereich:</TextBlock>
                <TextBlock Margin="10 0 0 0" Width="150" Text="{Binding Topic.Title}"></TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                <TextBlock>Schwierigkeit:</TextBlock>
                <ComboBox Margin="10 0 0 0" Width="150" ItemsSource="{Binding Difficulties}" SelectedItem="{Binding Competence.Difficulty}"></ComboBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                <TextBlock>Links zu Resourcen:</TextBlock>
                <TextBox Margin="10 0 0 0" Text="{Binding Competence.Links}"></TextBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10 0 0 0">
                <Button Margin="0 20 0 0" Command="{Binding SaveAndClose}">Speichern und Schliessen</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
